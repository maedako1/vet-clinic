{
  "name": "vet-clinic",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "next": "16.2.4"
  },
  "scripts": {
    "dev": "cd web && npm run dev",
    "build": "cd web && npm run build",
    "start": "cd web && npm run start",
    "lint": "cd web && npm run lint"
  }
}

